#tuples is immutable data type in python. It means once we create a tuple, we cannot modify it. Tuples are defined by enclosing the elements in parentheses ().

t = (1, 2, 3, 4, 5)

t[2] = 10 # This will raise an error because tuples are immutable
